package org.owasp.oag.config.configuration;

import java.util.HashMap;

/**
 * A specialized HashMap for storing login provider configuration settings.
 * This class extends HashMap to hold key-value pairs of configuration settings
 * specific to different login providers (like OAuth, OIDC, etc.).
 * The actual keys and values will depend on the specific login provider implementation.
 */
public class LoginProviderSettings extends HashMap<String, Object> {

}
